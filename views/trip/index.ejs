<%- include('../partials/header') %>
<table>
  <thead>
    <tr>
      <th>All Road Trips from Starting From NAME</th>
    </tr>
  </thead>
  <tbody>
     
    <% trip.forEach(function(t) { %>
      <tr>
        <td><%= t.tripStartEnd %></td>
        <td><a href="/trip/<%= t._id %>">DETAILS</a></td>
        <!-- <td><a href="/trips/t._id/new">ADD/EDIT TRIP DETAILS</a></td> -->
      <% }); %>
    </tbody>
  </table>
  
<%- include('../partials/footer') %>